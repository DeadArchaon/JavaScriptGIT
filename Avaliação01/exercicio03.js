var titulo = "Isto é um título longo de um livro";

console.log(titulo.length);

titulo = titulo.replace("livro","diário");

let junta = titulo + " mas não faz mal porque não é uma seca";
console.log(junta);

console.log(titulo.slice(0,4));